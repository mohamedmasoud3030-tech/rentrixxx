name: CI / Typecheck, Lint & Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      VITE_SUPABASE_URL: https://example.supabase.co
      VITE_SUPABASE_ANON_KEY: test-anon-key
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm supabase:migration-evidence
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm build
      - run: pnpm --filter ./artifacts/rentrix run typecheck:test
      - run: pnpm --filter ./artifacts/rentrix test
      - name: Run financial tests
        id: financial-tests
        shell: bash
        run: |
          set +e
          pnpm --filter ./artifacts/rentrix run test:financials > /tmp/rentrix-financial-tests.log 2>&1
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          tail -n 160 /tmp/rentrix-financial-tests.log
          exit 0
      - name: Upload financial test diagnostics
        if: steps.financial-tests.outputs.status != '0'
        uses: actions/upload-artifact@v4
        with:
          name: rentrix-financial-test-diagnostics
          path: /tmp/rentrix-financial-tests.log
      - name: Fail on financial test errors
        if: steps.financial-tests.outputs.status != '0'
        run: exit 1
