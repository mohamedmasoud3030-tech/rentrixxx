/**
 * @deprecated Use `@/services/api/supabaseClient` directly.
 * Compatibility shim retained to resolve merge conflicts with legacy imports.
 */
export { supabase, getSupabaseClient } from './api/supabaseClient';
