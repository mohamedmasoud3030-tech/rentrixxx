# 🚀 قائمة مهام إصلاح وتطوير Rentrix (TODO List)

تم تجميع هذه القائمة بناءً على تحليلات **Opus 4.5 High Effort**، **GitNexus Audit**، ومنهجية **SWE-agent**.

## 🔴 أولوية قصوى (P0) - استقرار وأمان النظام
- [ ] **توحيد معمارية الخدمات:** التخلص النهائي من الطبقة القديمة (`legacy layer`) وتحويل كافة الاستدعاءات إلى المجلد الموديولار الجديد.
- [ ] **تفكيك God Module:** تقسيم ملف `useAppCoreImpl.tsx` (1600+ سطر) إلى خطافات (`Hooks`) منفصلة (Auth, Finance, Ops).
- [ ] **تفعيل التحقق الصارم من الأنواع:** تمكين `strictNullChecks` في `tsconfig` ومعالجة كافة الأخطاء الناتجة لضمان عدم حدوث انهيارات وقت التشغيل.
- [ ] **منع تكرار المنطق المالي:** توحيد دوال `runManualAutomation` و `generateNotifications` في مكان واحد لمنع تضارب الحسابات.

## 🟡 أولوية متوسطة (P1) - الأداء والموثوقية
- [ ] **تحسين أداء الواجهة:** تعميم استخدام `React.memo` و `useCallback` في جميع المكونات الثقيلة (ليس فقط في Finance).
- [ ] **توسيع نطاق الاختبارات الآلية:** إضافة اختبارات تغطية (Unit Tests) لجميع Edge Functions باستخدام منهجية SWE-agent.
- [ ] **تفعيل التخزين المؤقت (Caching):** تنفيذ استراتيجية حقيقية لـ `CACHE_TTL` في `supabaseDataService` لتحسين سرعة استجابة البيانات.
- [ ] **تنظيف الكود الميت:** حذف المجلدات والملفات غير المستخدمة مثل `src/features/auth/LoginPage.tsx` لتجنب الارتباك.

## 🔵 أولوية عادية (P2) - تجربة المستخدم والتحسينات
- [ ] **توثيق الـ API:** إنشاء وثائق Swagger/OpenAPI لجميع Edge Functions لتسهيل التكامل المستقبلي.
- [ ] **تحسين نظام الإشعارات:** إضافة إشعارات فورية (Push Notifications) باستخدام Web Push API.
- [ ] **لوحة تحكم التحليلات:** بناء واجهة رسومية متقدمة لعرض البيانات المالية والعقارية باستخدام مكتبات الرسوم البيانية.

---
*ملاحظة: يتم تحديث هذه القائمة تلقائياً بواسطة سكريبت `auto_repair_runner.py` كلما تم إنجاز مهمة أو اكتشاف تحسين جديد.*
